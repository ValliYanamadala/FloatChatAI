# Data Workspace

This directory is reserved for ARGO data-processing work.

- `raw/`: local raw data inputs. Contents are ignored by git.
- `processed/`: local generated outputs. Contents are ignored by git.
- `scripts/`: future ETL and validation scripts.

Do not commit raw ARGO datasets or generated measurement artifacts.
