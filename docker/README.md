# Docker

Placeholder for future Dockerfiles and container support files.

The root `docker-compose.yml` currently defines infrastructure profiles for planned PostgreSQL/PostGIS and ChromaDB services only.
